apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: createuser
data:
  POSTGRES_DB: createUserPragma
  POSTGRES_HOST: db-auth
  POSTGRES_PORT: "5432"
  SPRING_PROFILES_ACTIVE: docker
