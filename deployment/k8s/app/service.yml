apiVersion: v1
kind: Service
metadata:
  name: createuser-service
  namespace: createuser
spec:
  selector:
    app: createuser-app
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort
