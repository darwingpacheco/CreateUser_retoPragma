app:
  ports:
    - "8081:8081"

spring:
  application:
    name: createUserPragma
  r2dbc:
    url: r2dbc:postgresql://db-auth:5432/createUserPragma
    username: postgres
    password: 1005076148
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

adapter:
  r2dbc-postgresql:
    host: db-auth
    port: 5432
    database: createUserPragma
    schema: public
    username: postgres
    password: 1005076148

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      probes:
        enabled: true
